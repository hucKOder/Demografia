<div align="center">
  <h3>Informácie o narodeniach v meste <%=@town%> od roku 2005</h3>
</div>

<script type="text/javascript">
    google.charts.load('current', {'packages':['corechart']});
    google.charts.setOnLoadCallback(drawChartSum);

    function drawChartSum() {

        var myJsArray = <%=raw @arr%>;
        var data = google.visualization.arrayToDataTable(myJsArray);

        var options = {
            title: 'Počet narodených v priebehu rokov',
            curveType: 'function',
            hAxis: { format: ''},
            vAxis: { format: 'decimal'},
            legend: { position: 'bottom' }
        };

        var chart = new google.visualization.LineChart(document.getElementById('sum'));

        chart.draw(data, options);
    }
    google.charts.setOnLoadCallback(drawChartDeath);

    function drawChartDeath() {


        var myJsArray = <%=raw @in_marriage%>;
        var data = google.visualization.arrayToDataTable(myJsArray);

        var options = {
            title: 'Počet narodených v manželstve a počet narodených',
            curveType: 'function',
            hAxis: { format: ''},
            vAxis: { format: 'decimal'},
            legend: { position: 'bottom' }
        };

        var chart = new google.visualization.LineChart(document.getElementById('number'));

        chart.draw(data, options);
    }
</script>

<div class="container" align="center">
  <div class="row">
    <div id="number" class="graph"></div>
    <div class="graph hideme" id="sum"></div>
  </div>
</div>
